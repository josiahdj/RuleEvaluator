namespace EligibilityRuleEvaluator {
	
	// Keep this in sync with [dbo].[Eligibility_Type] entity
	public enum EligibilityTypeEnum {
		Unknown = 0, // default
		EligibilityType1 = 1,
        EligibilityType2 = 2,
	}
}